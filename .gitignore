# Secrets — never commit
server/config.yaml
server/.env

# User data — version snapshots
server/data/

# Logs
*.log

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Font source files — only the subset woff2 in docs/fonts/web/ is shipped;
# full OTF/TTF source dirs are kept locally but not committed (hundreds of MB).
docs/fonts/ChillDuanHeiSong_v1.2/
docs/fonts/叁慕斜宋体/

# Tooling
.worktrees/

# macOS
.DS_Store
