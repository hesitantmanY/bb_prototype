# Brand-building 工作坊平台

配套的品牌策划工作坊平台：5 个工作坊（W1 品牌资产 → W2 目标市场 → W3 价值主张 → W4 营销组合 → W5 策划书），每个工作坊由若干步骤组成，使用者逐步产出策划文档。

## Language

**最小可交付（MVO）**:
每个步骤必须产出的最小成果清单（若干带通过条件的检查项），用于判断"这一步做完了没有"。全过 = 本步完成。
_Avoid_: 校验卡、门槛、checklist

**步间 CTA**:
本步最小可交付全过后，出现在步骤底部的「下一步：{目标步骤名} →」按钮，跳转到本工作坊的下一步骤。末步不出现（出口由跨坊 CTA 承担）。
_Avoid_: 下一步按钮、跳转键

**跨坊 CTA**:
工作坊末步最小可交付全过后，出现在底部的「{罗马数字}. {工作坊名} →」按钮，跳转到下一工作坊的首步。W5 无下游，不出现。
_Avoid_: 闭环按钮、坊间跳转

**闸门语义**:
最小可交付是步间 CTA 与跨坊 CTA 的唯一显隐闸门：全过即显示、未过则留空。CTA 是"过后的捷径"，不是"禁行栅栏"——步骤导航始终可用，未过步也可手动进入下一步骤（只提醒、不阻断）。
_Avoid_: 锁定、解锁、关卡

**步骤重渲染**:
步骤内容依赖上游步骤的状态（例如"评估候选市场"的闸门依赖"构建评估体系"保留的市场数），因此每次切换步骤都会以当前状态重新渲染目标步骤，旧渲染不会残留。
_Avoid_: 缓存、刷新、失效

**档案名**:
当前正在编辑的策划档：来源 = `state.meta.loadedFrom`（History.load 写入快照名），或在案例中时 = `state.meta.demoCase`。无源时（如全新工作区/重置后）顶栏「当前：xxx」标签隐藏，导出 MD 回退 `brand-workshop.md`。导出文件名 = `{档案名}-brand-workshop.md`，文档第一行标题 = `# {档案名}`。autosave 叠加改动不影响该绑定；顶栏的 ✎ 按钮直接调 History.rename，列表同步。
_Avoid_: 项目名、工程名、存档版本名

**AI 任务**:
一次按钮点击发起的后台生成过程，可含多次 LLM 调用；全局同一时刻至多一个，进行中时其他 AI 按钮被锁。
_Avoid_: 后台进程、请求

**多单元任务**:
含多次 LLM 调用的 AI 任务（流水线、逐项评分、批量润色）。只有多单元任务有暂停态；单调用任务生成中仅可中止。
_Avoid_: 长任务、批量任务

**三态按钮**（2026-09-01）:
AI 按钮的统一状态机：初始 →（点击）生成中 →（点击主体）已暂停 →（再点击主体）中止回初始。生成中另有 × 直接中止。每一态按钮只有一个主动作，没有「继续」。
_Avoid_: 暂停/继续切换、恢复按钮

**已暂停**:
任务在调用边界挂起的状态；进行中的那一次调用会跑完才停。唯一出口是中止。
_Avoid_: 等待中、待恢复

**中止**:
停止任务并回到初始态；已完成单元及其写入档案的数据保留，不回滚。
_Avoid_: 取消（暗示数据作废）、放弃

**断点续跑**:
已完成单元记录在案；中止或失败后重新点生成时跳过已完成部分、只补缺。是删除「继续」后恢复进度的唯一途径。
_Avoid_: 续传、断点恢复

## Work2 目标市场选择

**权重**:
指标在所属轴（市场吸引力 / 业务竞争力）内的重要性，分两级：一级权重 = 一级维度在轴内占比，二级权重 = 指标在所属一级内占比。
_Avoid_: 分数、系数、占比

**有效权重**:
单个二级指标在轴内的实际贡献 = 一级权重 × 二级权重；Delphi 收敛后即等于收敛权重。
_Avoid_: 合计权重、复合权重

**收敛权重**:
多视角（persona）对同一指标所给权重的均值，同轴内归一化后的结果；收敛后回填为两级存储权重，成为评分与矩阵的唯一权威。
_Avoid_: 最终权重（易与「手改后的当前权重」混淆）、finalWeights（仅作展示记录）

## Work3 价值主张

**语料**:
任何可作为证据的文本来源（评论、访谈、客服记录、Work 1 开放题答案等）；卖点挖掘的输入单位。
_Avoid_: 评论文本（只是语料的子集）、用户发言

**真实语料 / 模拟语料**:
真实语料 = 用户提供或上游接入的原文；模拟语料 = 基于用户画像与场景合成的示意文本。两类都算语料，但来源与可信度不同，模拟必须标注。
_Avoid_: 假数据、编造的数据（带贬义）

**真实 LDA / 模拟 LDA**:
真实 LDA = 后端对真实语料做主题建模；模拟 LDA = LLM 产出同构结果（主题/关键词/代表文档），可在零语料下运行。二者形态一致，来源不同。
_Avoid_: 假建模、LDA 模拟（与「模拟语料」混用）

**双通道**:
卖点挖掘的两条语料来源：真实语料（用户提供/上游接入）与模拟语料（画像生成）。模拟语料在真实不足时补足到可运行，真实足量时作为补充继续参与建模；来源构成始终可见。
_Avoid_: 有无评论、备用通道、降级通道

**语料构成**:
建模语料中真实与模拟的数量与占比（如"真实 12 + 模拟 9"）；构成必须在界面与导出中可见。
_Avoid_: 混合比例（只指占比，丢了数量）

**样本补足**:
真实语料不足 3 条时，由系统基于画像与场景生成模拟语料补齐到可运行数量；已有真实文本保留为种子，不丢弃。
_Avoid_: 编语料、凑数（带贬义）

**负面语料**:
模拟语料中必须包含的负面/抱怨文本（未满足的需求、失败体验、犹豫、竞品对比失望、不推荐理由），默认开启；它是痛点地图证据的主要来源。
_Avoid_: 差评（范围更窄，只指电商差评）、吐槽文本（口语化，缺分析含义）

**支撑证据**:
卖点"这个痛是真的、方案能解决"的可验证依据：语料原文摘录（模拟语料标 [模拟]）、量化统计（N 篇评论提及）、或第三方/内部依据；没有资料时写「内部策略，无评论」，不编造。
_Avoid_: 卖点描述（是方案本身不是证据）、自夸文案

**价值主张候选**:
主张步骤中由 AI 生成、可增删改的候选句集合（3–5 条）；保留多条用于比较与备选，不是最终交付物。
_Avoid_: 备选主张（易与「选定」混淆）

**选定价值主张**:
从候选集中单选的一个主主张（`chosenValueText`），定位句、人格/Slogan、W4、W5 与导出都以它为唯一权威；候选池不设多选（2026-08-29 共识：维持单选）。
_Avoid_: 主选、首选（不表达"已收敛"）

## Work4 营销组合（4P）

**步级 AI 起草**（2026-09-01，取代段落采纳流）:
4P 每步顶部唯一一个「AI 起草」主按钮：一次 LLM 调用同时产出叙事正文与一个字段 JSON 对象；JSON 容错解析后整组覆盖该步表单字段（未含 key 不动），叙事存 `aiResult` 自动全部采纳、只读展示。该步已有内容时点按钮弹「整体替换」确认（workshop123 式）。
_Avoid_: 段落采纳、逐字段按钮（旧模型）

**表单即真相源**:
`summaryText` / 导出 / Work5 一律从 4P 表单字段拼接；段落叙事只作阅读参考，不参与导出。用户在表单里的修改直接生效。
_Avoid_: 采纳段落拼接（旧模型）

**promotion 为末步**:
Work4 无汇总步（summary 已删除，汇总由 Work5 承担）；promotion 的 mvo 全过后，步底显示「进入 Workshop 5 →」跨坊 CTA。
_Avoid_: 4P 汇总、一键同步到 Work5

**上下文引用**:
步级 AI 起草按钮上方的折叠区「▾ 基于：SBU=X · 市场=Y · 价值主张=Z · 定位=W · Slogan=Q」，展开看完整发给 LLM 的 context payload。目的是让用户在点「重新生成」前先确认 AI 看的是不是最新上游数据。
_Avoid_: 上下文（太宽泛）、上游摘要（与上游无关的就不算）

## Work2 市场矩阵（2.3）

**切分线**:
把平面切成四个象限的门槛值。用户手动设定优先；未设定时自动取该轴评分区间的中点 (min+max)/2（2026-09-01，取代中位数——中位数在 3 点场景必然穿过中间市场的坐标，象限判定沦为平局裁决）。仅 1 个保留市场时不设切分线、不判象限。
_Avoid_: 中位数切分（旧默认）

**四象限**:
明星（双高·重点投入）/ 潜力（吸引力高竞争力低·补能力）/ 产能（竞争力高吸引力低·选择性收割）/ 双低（放弃）。象限是分析结论，只描述市场在矩阵中的位置。
_Avoid_: 把象限当作资源分配指令

**三档决策**:
tier1 主战场（约 80% 资源）/ tier2 观察收割 / tier3 放弃——是资源分配决策，与象限解绑。无明星时 tier1 仍可有意识地选择非明星市场，但理由必须写明取舍（补什么能力/放弃什么）。
_Avoid_: tier1 必须是明星（旧隐含规则）、矩阵自动指定主战场

**痛点绑定**:
备选卖点与痛点地图条目的关联方式二选一：`painId` 关联（权威，痛点列显示地图原文、证据可自动带入）或自定义文本（painId 为空，用户手输手选）。绑定动作只认高置信匹配（精确/包含）；候选 pain 文本与地图对不上时保持自定义，不得静默代绑。
_Avoid_: 痛点引用（不表达"绑定有权威源"）、自动匹配（不区分置信档）

## Work5 策划书

**进入即同步**:
用户进入 W5 时，自动把上游最新成果带入策划书；覆盖前自动保存可恢复版本。上游变化后再次进入会刷新。
_Avoid_: 刷新、重新汇总（旧手动语义）

**证据块**:
W5 中直接引用上游工作坊结果的区块（矩阵、排名表、决策卡、痛点地图、渠道结构等），与上游共享 state 实时一致；修改去上游，W5 只读展示。
_Avoid_: 截图、静态图表

**成稿检查**:
W5 中显示上游四坊完成状态的面板，逐坊提供「去完成」入口，不硬卡 W5 自己的完成判据。
_Avoid_: 校验卡、门槛

**结构化重组**:
4P/4C 导入时按 Work4 表单字段拼成「标题 + 要点 + 小表」的文档块，而不是原样贴入文本。
_Avoid_: 复制粘贴、直接贴入

**AI 改写格式约束**:
AI 润色/改写输出必须保持文档块结构（标题/要点/表格），不得破坏格式。
_Avoid_: 自由改写

## 跨工作坊

**上游快照**:
归一化的上游成果只读视图（SBU / 目标市场三档 / 价值主张 / 定位 / 4P / 成稿状态），供 W5 自动同步、成稿检查、健康检查等共享；修改永远去上游工作坊。
_Avoid_: 数据源、上下文对象

**上游成稿状态**:
逐坊 MVO 完成判据的单一求值结果；W5 成稿检查面板、导出摘要与健康检查共用同一份判据。
_Avoid_: 完成率（语义含混，未表达「各步检查项全过」）

**唯一出口**:
跨坊读取某个上游概念（如目标市场三档）时，只允许一个归一化出口，调用者不直读上游工作坊的内部 schema 与旧数据回退分支。
_Avoid_: 数据源、适配器（实现词）
